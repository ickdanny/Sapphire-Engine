let waitSecs = 0.45;
let turnTicks = 45;
let turnAngleOffset = diff(30.0, 60.0, 90.0);

secTimer(waitSecs);
let turnAngle = getAngle() + turnAngleOffset;

shiftVelocityTurnPeriod(
    getSpeed(),
    turnAngle,
    turnTicks
);