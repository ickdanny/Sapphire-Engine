(normalVal, hardVal, lunaticVal){
    let difficulty = getDifficulty();
    if(difficulty == 1){
        return normalVal;
    }
    if(difficulty == 2){
        return hardVal;
    }
    if(difficulty == 3){
        return lunaticVal;
    }
    error("difficulty is not 1, 2, or 3");
}