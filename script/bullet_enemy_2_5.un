let baseDirection = 90;
let directionRange = diff(10.0, 15.0, 20.0);
let accel = 1.0;
let dragLow = 0.0001;
let dragHigh = 0.002;

let drag = random(dragLow, dragHigh);
let direction = baseDirection + random(
    -directionRange,
    directionRange
);

gravity(direction, accel, drag);