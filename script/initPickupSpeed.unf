(){
    let initSpeedMulti = 0.7;
    let initSpeedBase = 1.5;

    let yCoord = getY();
    let heightRatio = (yCoord - gameOffset().y)
        / gameHeight();
    let speedMulti = 1 
        + (heightRatio * initSpeedMulti);
    return initSpeedBase * speedMulti;
}