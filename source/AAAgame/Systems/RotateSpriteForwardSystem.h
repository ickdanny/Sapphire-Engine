#ifndef ROTATESPRITEFORWARDSYSTEM_H
#define ROTATESPRITEFORWARDSYSTEM_H

#include "SystemCommon.h"

/*
 * rotates the entity's sprite to point in the
 * direction it is moving
 */
void rotateSpriteForwardSystem(
    Game *gamePtr,
    Scene *scenePtr
);

#endif